package com.everestengineering.model;

public class Car extends Vehicle {
    public Car(String registrationId, String color, SlotType slotType) {
        super(registrationId, color, slotType);
    }
}
